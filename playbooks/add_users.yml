---
- name: Adding users
  hosts: all
  become: true

  tasks:
  - name: Add user
    user:
      name: bob
      state: present
      create_home: yes
      shell: /bin/bash
      comment: Bob Bobbers


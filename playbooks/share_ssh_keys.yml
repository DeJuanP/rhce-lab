---
- name: Share ssh keys with managed nodes
  hosts: all
  become: true

  tasks:
  - name: copy keys from control node
    ansible.posix.authorized_key:
      user: ansible
      state: present
      key: "{{ lookup('file', '/home/ansible/.ssh/id_ed25519.pub') }}"


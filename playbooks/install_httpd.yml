---
- name: Install and start httpd
  hosts: webservers
  become: true

  tasks:
  - name: Install httpd
    dnf:
      name: httpd
      state: present
      update_cache: true

  - name: Restart httpd
    service:
      name: httpd
      state: started
      enabled: true 
